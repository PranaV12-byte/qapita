/**
 * The body is intentionally not word-limited. These character values protect
 * the JSON, PDF, and email transport boundaries without cutting prose in half.
 */
export const MAX_GENERATED_BODY_CHARACTERS = 40_000;
export const TARGET_GENERATED_BODY_CHARACTERS = 38_000;
